# Supabase — Project Settings → API. Safe to ship (public anon key + URL).
EXPO_PUBLIC_SUPABASE_URL=https://YOUR-PROJECT-REF.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=your-anon-public-key

# NEVER put the service_role key in an EXPO_PUBLIC_* var — it bypasses RLS.
